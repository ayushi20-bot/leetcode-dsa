Problem: Pivot Index (LC 724)

Pattern: Prefix Sum

Approach:
Calculate totalSum of the array.

Maintain leftSum while traversing.

For each index:
rightSum = totalSum - leftSum - nums[i]

If leftSum == rightSum:
return i

Otherwise:
leftSum += nums[i]

Time Complexity: O(n)

Space Complexity: O(1)
